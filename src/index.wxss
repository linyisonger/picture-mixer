:host {
  display: inline-flex;
  overflow: hidden;
  width: 600rpx;
  height: 600rpx;
}
.pm-canvas {
  position: relative;
  display: inline-flex;
}
.pm-canvas,
.canvas {
  width: inherit;
  height: inherit;
}
.mixer,
.operate,
.background {
  position: absolute;
}
